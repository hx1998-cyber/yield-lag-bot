# Project YIELD-LAG non-secret local settings.
# Secrets and paid-data credentials are intentionally absent for M1.

live_trading: false
paper_trading: true
max_order_usd: "10"
max_position_usd: "50"
max_daily_loss_usd: "20"
max_latency_ms: 300
stale_data_ms: 500
kill_switch_on_error: true

crypto_symbols:
  - BTCUSDT
  - ETHUSDT

hyperliquid_symbols:
  - BTC
  - ETH

cme_symbols:
  - ZT
  - ZF
  - ZN
  - TN

hyperliquid_network: mainnet
hyperliquid_mainnet_ws_url: wss://api.hyperliquid.xyz/ws
hyperliquid_testnet_ws_url: wss://api.hyperliquid-testnet.xyz/ws
